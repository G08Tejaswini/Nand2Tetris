
/**
 * Or gate:
 * if (a or b) out = 1, else out = 0 
 */
CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    Not(in= a, out=outa );
    Not(in= b, out=outb );
    Nand(a=outa , b= outb, out=out );
}
